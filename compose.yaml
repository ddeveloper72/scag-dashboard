services:
  dashboard:
    build:
      context: .
      args:
        PUBLIC_AZURE_CLIENT_ID: ${PUBLIC_AZURE_CLIENT_ID}
        PUBLIC_AZURE_TENANT_ID: ${PUBLIC_AZURE_TENANT_ID}
        PUBLIC_DASHBOARD_URL: ${PUBLIC_DASHBOARD_URL}
        PUBLIC_EHEALTH_PORTAL_URL: ${PUBLIC_EHEALTH_PORTAL_URL}
        PUBLIC_EHEALTH_PORTAL_LOGIN_PATH: ${PUBLIC_EHEALTH_PORTAL_LOGIN_PATH}
        PUBLIC_OPENNCP_PORTAL_URL: ${PUBLIC_OPENNCP_PORTAL_URL}
        PUBLIC_OPENNCP_PORTAL_LOGIN_PATH: ${PUBLIC_OPENNCP_PORTAL_LOGIN_PATH}
    ports:
      - "4321:80"